---
import Footer from "../components/Footer.astro";
import Header from "../components/Header.astro";

export interface Props {
	title?: string;
}

const { title } = Astro.props as Props;
---

<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="utf-8" />
		<meta
			name="viewport"
			content="width=device-width, initial-scale=1, shrink-to-fit=no"
		/>

		{
			title ? (
				<title>{title} | Steve Thorne</title>
			) : (
				<title>Steve Thorne</title>
			)
		}

		<meta name="description" content="Senior Programmer at Writable, Inc." />
		<meta
			name="keywords"
			content="steven thorne, steve thorne, software, full-stack, developer, writable"
		/>

		<meta property="og:site_name" content="Steve Thorne" />
		<meta
			property="og:description"
			content="Senior Programmer at Writable, Inc."
		/>
		<meta property="og:type" content="website" />
		<meta property="og:title" content="Home" />
		<meta
			property="og:image"
			content="https://avatars0.githubusercontent.com/u/3374032?v=4"
		/>
		<meta property="og:image:type" content="image/jpeg" />
		<meta property="og:image:width" content="460" />
		<meta property="og:image:height" content="460" />
		<meta property="og:image:alt" content="Steve Thorne" />
		<meta property="theme-color" content="#ff6500" />

		<meta name="generator" content={Astro.generator} />

		<link rel="preconnect" href="https://fonts.gstatic.com/" />
		<link
			href="https://fonts.googleapis.com/css2?family=Open+Sans:wght@400;700&family=Roboto:wght@400;700&text=ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz%26&display=swap"
			rel="stylesheet"
		/>
		<link href="https://www.sthorne.dev/" rel="canonical" />
	</head>
	<body>
		<Header />
		<main>
			<slot />
		</main>
		<Footer />
		<style>
			:root {
				--font-size-base: clamp(1rem, 0.34vw + 0.91rem, 1.19rem);
				--font-size-lg: clamp(1.2rem, 0.7vw + 1.2rem, 1.5rem);
				--font-size-xl: clamp(2.44rem, 2.38vw + 1.85rem, 3.75rem);

				--color-text: hsl(12, 5%, 4%);
				--color-bg: hsl(10, 21%, 95%);
				--color-border: hsl(17, 24%, 90%);
				 line-height: 1.5;
			}

			body {
				display: flex;
				flex-direction: column;
				align-items: center;
				min-height: 100vh;
				background-color: rgba(26, 32, 44, 1);
				color: #fff;
				font-family: Open Sans, sans-serif;
				margin: 0;
				line-height: inherit;
			}

			main {
				flex: 1 1 auto;
				padding: 2rem 0;
				margin: 0 1.5rem;
				font-size: 1.125rem;
				max-width: 56rem;
			}

			:global(*, ::after, ::before) {
				box-sizing: border-box;
			}

			:global(p, h1, h2, h3, h4, h5, h6, ul, ol) {
				margin: 0;
			}

			:global(h1, h2, h3, h4, h5, h6) {
				font-size: inherit;
				font-weight: inherit;
			}

			:global(a) {
				color: inherit;
				text-decoration: inherit;
			}

			:global(svg) {
				vertical-align: middle;
			}

			@media (min-width: 768px) {
				main {
					margin: 0 4rem;
				}
			}

			@media (min-width: 1024px) {
				main {
					padding: 3rem 0;
					margin: 0 6rem;
				}
			}
		</style>
	</body>
</html>
